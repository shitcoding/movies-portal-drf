# Development environment database settings
# ------------------------------------------------------------------------------
# PostgreSQL credentials
POSTGRES_DB=movies_database # Change the value
POSTGRES_USER=app # Change the value
POSTGRES_PASSWORD=123qwe # Change the value
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
